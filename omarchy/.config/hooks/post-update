#!/bin/bash
# Omarchy Post-Update Hook
# Runs after tema sets a new wallpaper/theme

echo "🔄 Running post-update hook..."

# Update Vencord QuickCSS from pywal colors
if [ -f "$HOME/.local/bin/pywal-discord" ]; then
    echo "  → Updating Discord theme..."
    "$HOME/.local/bin/pywal-discord"
else
    echo "  ⚠️  pywal-discord script not found"
fi

# Add more custom theme updates here
# Example: Update other apps that support dynamic theming

echo "✅ Post-update hook completed"
